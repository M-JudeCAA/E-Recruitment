# ==========================================================================
# LOCAL / DEVELOPMENT SECRETS - do not commit this file (it is in .gitignore)
# Replace SMTP_* and DB credentials with real values before any shared or
# production deployment. Rotate JWT_SECRET if this file is ever exposed.
# ==========================================================================

# Database (MySQL) - Aiven-managed cloud instance
DATABASE_URL="mysql://avnadmin:AVNS_TyywatHFdStQT605ZGb@mysql-32b198ce-caa-48e5.l.aivencloud.com:17434/defaultdb?ssl-mode=REQUIRED"

# Auth - randomly generated, safe for local/dev use only
JWT_SECRET="ceeb300622db3fccb31f22b99bf7ac08ee7bebd4028f6c4d1ed1aaab51bc5653a3fb5a8b1b830f688c21c1b9ee98164a"
JWT_EXPIRES_IN="8h"

# Internal candidate domain check
INTERNAL_EMAIL_DOMAIN="caa.co.ug"

# App
PORT=4000
FRONTEND_URL="http://localhost:5173"

# Email (SMTP) - point at UCAA's mail relay, or any SMTP provider.
# Left as placeholders; registration/notification emails will fail until set.
SMTP_HOST="smtp.gmail.com"
SMTP_PORT=587
SMTP_USER="recruithrcaa@gmail.com"
SMTP_PASS="aqpr ysao msoj vxto"
SMTP_FROM="UCAA e-Recruitment <recruithrcaa@gmail.com>"

# File storage - local path when running via Docker Compose (mounted as a volume)
UPLOAD_DIR="./uploads"
